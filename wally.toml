[package]
name = "ldgerrits/bootstrapper"
description = "A lightweight, deterministic bootstrapper for Roblox. Find modules, control the execution flow, and bind to (RunService) events with memory tracking."
version = "1.0.12"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
exclude = ["**"]
include = ["src", "src/*", "wally.toml", "default.project.json", "LICENSE.md"]
