[package]
name = "ldgerrits/bootstrapper"
description = "A lightweight and unopinionated lifecycle manager for Roblox. Find and load modules, run their functions, and bind them to RunService events."
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
exclude = ["**"]
include = ["src", "src/*", "wally.toml", "default.project.json", "LICENSE.md"]
